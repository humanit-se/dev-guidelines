/*
 * Custom config 
 * 
 * To use custom configuretions, copy this file into config/custom.js
 * and add any of your custom configs there. They will get priority
 * over any development/production configurations. 
 * 
 * NOTE! Make sure "config/custom.js" is in .gitignore, as this file should
 * never end up into git repository
 */
var config = {};


// Add your configurations here (fe. "config.thing = 1;")


module.exports = config;
